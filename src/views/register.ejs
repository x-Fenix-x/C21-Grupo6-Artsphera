<!DOCTYPE html>
<html lang="en">
<%- include('partials/head',{title: "Artesphera | Registro" }) %>

	<body>
		<%- include('partials/header') %>
			<main>
				<section class="mainContent">
					<article class="firstArticle">
						<div class="loginTop">
							<h1>Crea una cuenta</h1>
							<p>¡Ey! Regístrate ingresando tus datos.</p>
						</div>
						<form action="" class="form" method="POST" id="formAdd">
							<input type="text" id="name" name="name"
								class="form-control <%= locals.errors && (locals.errors.name && 'is-invalid') %>"
								placeholder="Ingresa tu nombre" value="<%= locals.old && (old.name && old.name) %>" />
							<small class="text-danger" id="msgError-name">
								<%= locals.errors && (errors.name && errors.name.msg) %>
							</small>
							<input type="text" id="surname" name="surname"
								class="form-control <%= locals.errors && (locals.errors.surname && 'is-invalid') %>"
								placeholder="Ingresa tu apellido"
								value="<%= locals.old && (old.surname && old.surname) %>" />
							<small class="text-danger" id="msgError-surname">
								<%= locals.errors && (errors.surname && errors.surname.msg) %>
							</small>
							<input type="email" id="email" name="email"
								class="form-control <%= locals.errors && (locals.errors.email && 'is-invalid') %>"
								placeholder="Ingresa tu email" value="<%= locals.old && (old.email && old.email) %>" />
							<small class="text-danger" id="msgError-email">
								<%= locals.errors && (errors.email && errors.email.msg) %>
							</small>
							<input type="password" id="password" name="password"
								class="form-control <%= locals.errors && (locals.errors.password && 'is-invalid') %>"
								placeholder="Ingresa tu contraseña"
								value="<%= locals.old && (old.password && old.password) %>" />
							<small class="text-danger" id="msgError-password">
								<%= locals.errors && (errors.password && errors.password.msg) %>
							</small>
							<input type="password" id="password2" name="password2"
								class="form-control <%= locals.errors && (locals.errors.password2 && 'is-invalid') %>"
								placeholder="Repita tu contraseña"
								value="<%= locals.old && (old.password2 && old.password2) %>" />
							<small class="text-danger" id="msgError-password2">
								<%= locals.errors && (errors.password2 && errors.password2.msg) %>
							</small>
							<div>
								<small class="text-danger" id="msgError-empty">
								</small>
							</div>

							<button type="submit">Registrarse</button>
						</form>
						<p>O puedes crearla también con:</p>
						<div class="loginLinks">
							<a href="#" class="loginGoogle">
								<i class="fa-brands fa-google"></i>
								<p>Google</p>
							</a>
							<a href="#" class="loginApple">
								<i class="fa-brands fa-apple"></i>
								<p>Apple ID</p>
							</a>
							<a href="#" class="loginFacebook">
								<i class="fa-brands fa-facebook"></i>
								<p>Facebook</p>
							</a>
						</div>
						<div class="loginNoAcc">
							<p>¿Ya tienes una cuenta?</p>
							<a href="/users/login">Entra ahora</a>
						</div>
					</article>
				</section>
			</main>
			<%- include('partials/footer') %>
				<script src="/javascripts/register.js"></script>
	</body>

</html>